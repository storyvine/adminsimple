.links
  - if controller_name != 'sessions'
    = link_to "Sign in", adminsimple.send(:"new_#{resource_name}_session_path")

  - if devise_mapping.recoverable? && controller_name != 'passwords'
    = link_to "Forgot your password?", adminsimple.send(:"new_#{resource_name}_password_path")

  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = link_to "Didn't receive confirmation instructions?", adminsimple.send(:"new_#{resource_name}_confirmation_path")

  -#- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
  -#  = link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name)

  .omniauth
    - if devise_mapping.omniauthable?
      - resource_class.omniauth_providers.each do |provider|
        = link_to "Sign in with #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider)
